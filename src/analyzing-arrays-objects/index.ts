/**
 * Objects:
 * - Work well when you don't need order
 * - insertion, removal and access is great and constant time
 * - searching is not constant
 * Entries flattens things
 */

/**
 * Arrays:
 * - Use them when you need order
 * - Singly-Linked List and Doubly-Linked List may even be better for order
 * - comes at a cost with insertion and removal
 * - have to reindex everything if adding to or removing from the beginning
 */

export {};
